apiVersion: v1
kind: Service
metadata:
  name: valkey-service
  namespace: mumnk8s
spec:
  type: ClusterIP
  selector:
    vm.kubevirt.io/name: valkey-vm
  ports:
    - name: valkey
      port: 6379
      targetPort: 6379